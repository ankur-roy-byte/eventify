server:
  port: 8080

spring:
  datasource:
    url: ${ORDERS_DB_URL:jdbc:postgresql://localhost:5432/orders}
    username: ${ORDERS_DB_USER:orders_app}
    password: ${ORDERS_DB_PASSWORD:CHANGE_ME_ORDERS_DB_PASSWORD}
  jackson:
    serialization:
      write-dates-as-timestamps: false
